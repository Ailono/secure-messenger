services:
  - type: web
    name: secure-messenger
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python web_server.py
    envVars:
      - key: PORT
        generateValue: false
